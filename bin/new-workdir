#!/usr/bin/env bash
set -euo pipefail

if [ $# -lt 1 ]; then
  echo "Usage: bin/new-workdir <branch> [directory]"
  exit 1
fi

branch="$1"
shift

dir="${1:-../valley-pike-$branch}"

git worktree add "$dir" "$branch"

(
  cd "$dir"
  bin/docker-setup
)
