<div class="space-y-5">
  <div>
    <p class="text-base font-semibold text-primary">1 of 5: Public Information</p>
    <p class="mt-1 text-sm text-primary/70">This information will be sent to all eligible drivers</p>
  </div>

  <%= render organization_field %>

  <%= render short_description_field %>

  <%= render ride_date_field %>

  <%= render driver_count_field %>

  <%= render driver_gender_field %>

  <%= render appointment_time_field %>
</div>
