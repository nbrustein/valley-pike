<%# FIXME: determine what fields the driver show page should display %>
<section class="surface-card">
  <div class="mb-6">
    <h1 class="text-3xl font-semibold tracking-tight text-primary"><%= @ride_request.short_description %></h1>
  </div>

  <p>Driver ride request show page — details TBD.</p>

  <%= link_to "Back", root_path, class: "text-sm font-semibold text-primary/70 transition hover:text-primary active:text-primary/80 mt-4 inline-block" %>
</section>
