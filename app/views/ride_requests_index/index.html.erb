<% columns = [ "Date", "Description" ] %>
<% columns << "Org" if show_ride_request_org_column? %>
<%= render(ResourceListPageComponent.new(title: "Ride Requests")) do %>
  <%= render(ResourceListComponent.new(columns:)) do %>
    <% @ride_requests.each do |ride_request| %>
      <%= render "ride_requests_index/ride_request_row", ride_request: %>
    <% end %>
  <% end %>
<% end %>
