<% editable = organization_edit_allowed?(organization) %>
<% row_path = editable ? edit_organization_path(id: organization.id) : organization_path(id: organization.id) %>
<tr class="border-b border-forest-900/10 cursor-pointer transition hover:opacity-85 active:opacity-70"
    data-organization-id="<%= organization.id %>">
  <td class="px-3 py-3 text-forest-900">
    <%= link_to row_path, class: "block" do %>
      <span><%= organization.name %></span>
    <% end %>
  </td>
  <td class="px-3 py-3 text-right text-forest-900">
    <%= link_to row_path, class: "inline-flex items-center" do %>
      <i class="fa-solid <%= editable ? "fa-pen-to-square text-forest-900/70" : "fa-eye text-forest-900/50" %>"></i>
    <% end %>
  </td>
</tr>
